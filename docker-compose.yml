version: '3.8'

services:
  backend:
    build: ./backend
    container_name: biomni-backend
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app/biomni_repo:$PYTHONPATH
      - BIOMNI_DATA_PATH=/app/data
    ports:
      - "8002:8000"
